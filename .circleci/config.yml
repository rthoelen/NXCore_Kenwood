version: 2
jobs:
  build:
    docker:
      - image: ubuntu:latest
    steps:
      - checkout
      - run:
          name: Compile the software
          command: cd NXCore_Kenwood; ./build_debian.sh
      - run:
          name: print the date
          command: date

